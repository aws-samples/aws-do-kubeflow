#Implement Logic to check for necessary pre-requisites before deploying kubeflow

export PREREQUISITES=false

#check if default storage class is configured
#check if mpijob.kubeflow crd is already installed
#check if istio is already installed

#export PREREQUISITES=true if everything is met